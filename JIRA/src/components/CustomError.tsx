import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CustomError = () => {
  return <Text>CustomError</Text>;
};

export default CustomError;

const styles = StyleSheet.create({});
